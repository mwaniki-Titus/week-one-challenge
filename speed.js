function calculateDemeritPoints(speed) {
    const speedLimit=80;
    const kmPerDemeritPoints=5;
    const maxDemeritPoints=20;

    if (speesd<speedLimit){
        console.log("ok");
        return;
    }
    const 
}